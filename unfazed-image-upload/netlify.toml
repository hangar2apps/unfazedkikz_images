[dev]
  framework = "create-react-app"
  targetPort = 3000
  publish = "build"
  functions = "functions"

[build]
  command = "npm run build"
  publish = "build"
  functions = "functions"
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200